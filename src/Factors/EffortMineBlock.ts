import BotState from "../Botstate";
import Factor from "../Factor";

export default class EffortMineBlock extends Factor<number> {
    block: string


    calculate(botState: BotState): number {
    }
}